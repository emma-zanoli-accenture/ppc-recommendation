PROMPT (copy-paste)

Read CLAUDE.md first — it is the source of truth for this project.

We are building "Recopilot", a scripted demo of an agentic platform for governing Board of Directors
recommendations, for the prospect PPC (PPC Group, the Greek electricity utility). This step is
foundations only.

Use plan mode. Propose a plan covering:
- Project scaffolding: Vite + React + TypeScript + Tailwind CSS.
- Dependencies: zustand, framer-motion, lucide-react.
- A clean folder structure: src/store, src/data (seed + agent scripts), src/agents (scripted
  engine), src/components (shared UI), src/personas (the 3 journey views), src/lib (types, utils),
  src/styles.
- A design system: define a polished, modern legal-tech / control-tower aesthetic. Set Tailwind
  theme tokens for a brand palette (deep ink/navy base, refined surfaces, one confident brand
  accent) PLUS a distinct "agentic accent" reserved only for AI elements. Define a status color
  system for the recommendation statuses in CLAUDE.md. Choose tasteful typography (a humanist sans
  for UI; consider a serif for formal legal/document content). Use the frontend-design skill.
- ESLint + Prettier, TypeScript strict mode.
- Put the client name "PPC" in a single configurable constant.

After I approve the plan: scaffold the project into the current directory, install everything,
apply the Tailwind theme tokens, create a minimal styled app shell placeholder (just a centered
brand lockup "Recopilot · for PPC Group" with the tagline "your agentic copilot for board
recommendations" so I can verify styling), and make `npm run dev` work. Do not build any journeys yet.