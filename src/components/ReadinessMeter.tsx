interface Props {
  score: number // 0–100
}

// Stub — full implementation in Step 3
export default function ReadinessMeter({ score: _score }: Props) {
  return (
    <div className="w-full h-2 bg-surface-overlay rounded-full">
      {/* To be built in Step 3 */}
    </div>
  )
}
