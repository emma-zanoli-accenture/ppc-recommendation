Re-read CLAUDE.md. Now build the data backbone. No UI screens yet — only types, store, seed,
and the scripted agent engine.

1. Types (src/lib/types.ts): model Recommendation exactly per CLAUDE.md (status flow, reviews per
   function, draftResolution mandatory, regulatoryRefs, readinessScore, directToChairman, auditLog,
   boardMeetingDate, bodDeadline). Add types for Comment, ReviewState, and an AgentRun (agent id,
   reasoning steps, streamed output, result).

2. Zustand store (src/store): single shared store as the source of truth for all personas.
   - Actions for the full lifecycle: createRecommendation, updateContent, sendToFunctions(subset),
     sendDirectToChairman(reason), addReviewComment, approveReview, returnForUpdate, acceptFinal,
     submitToSecretariat, runCompletenessCheck, generateBoDPack, submitToBoD, plus appendAuditLog.
   - Persist to localStorage AND sync across tabs via a `storage` event listener that rehydrates.
   - A resetDemo() action that clears storage and reseeds.

3. Seed data (src/data/seed.ts): ~11 realistic recommendations in MIXED statuses across business
   units (Procurement, Regulatory Affairs, Finance/Treasury, IT, Operations, ESG, Legal/Compliance,
   HR), consistent with a large electricity utility like PPC. Plausible English titles and content.
   Set a near-future board meeting date and compute bodDeadline (~2 working days before). The hero
   reco ("New cross-border energy trading agreement") is NOT seeded — it is created live during the
   demo.

4. Scripted agent engine (src/agents): Recopilot orchestrates 5 specialist agents from CLAUDE.md.
   Use plain descriptive names, no code names: Drafting Agent, Legal Review Agent, Finance Review
   Agent, Compliance Review Agent, Readiness Agent. For each, a typed script with: ordered reasoning
   steps (short chips, e.g. "Analyzing REMIT Art. 4 disclosure obligations"), a streamed text result,
   and any structured output (suggested template, comments, readiness score, BoD pack). Build a
   `useAgent` hook that plays a script: thinking → steps appear one by one → result streams in, with
   short simulated delays. It must be deterministic and instant-enough for stage use. Write the
   scripts for all five agents specifically for the hero golden path — a Greece–Romania cross-border
   energy trading agreement (use REMIT / EMIR / ACER / RAAEY, with MiFID II / HEnEx as secondary
   flags) so the content reads realistic for PPC.

Add a tiny temporary dev page/button to trigger each agent and to inspect the store, so I can
verify before we build the real screens. Keep it isolated; we'll remove it later.