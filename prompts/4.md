Re-read CLAUDE.md. Use plan mode to outline the Business Unit (Procurement) journey, then build it.
This is the hero journey; the patterns here will be reused, so get the interaction model right.

Build the full UJ1 flow against the shared store:

0. BU Dashboard (entry point): summary of this function's (Procurement) recommendations, stats by
   status (draft / under review / approved / submitted), and a "New Recommendation" button.

1. Create new recommendation: a business-need text field PLUS a "Use example" button that fills
   "New cross-border energy trading agreement". On submit, the Drafting Agent runs in an AgentPanel
   and generates a precompiled template: structure based on guidelines, the mandatory draft
   resolution, and regulatory scaffolding referencing REMIT / EMIR / ACER / RAAEY (with MiFID II /
   HEnEx as secondary flags), framed for PPC's Greece–Romania cross-border context.

2. Assisted draft: the user fills/edits content; the Drafting Agent suggests integrations and flags
   information gaps inline.

3. Send to control functions: a selector to choose any subset of Legal / Finance / Compliance.
   Also include — shown en passant, not emphasized — a "Send directly to Chairman" option that
   requires a mandatory reason. Sending sets status to "Under Review" and writes the audit log.
   (Stop the live narrative here; we go to UJ2 next, then return.)

4. Receive feedback: the Business Owner sees Legal comments / change requests, plus Finance and
   Compliance approvals, and Legal "Review completed (with comments)". (This becomes visible after
   UJ2 runs.)

5–7. Update & accept: integrate Legal comments with changes highlighted and resolved comments
   marked; then final acceptance sets status to "All Reviews Completed". Support iteration (option
   to send back to Legal again) but default to the standard single-cycle case.

9. Submit to Corporate Secretariat → status "Submitted to Secretariat".

Everything writes to the shared store so the other personas see the live state. Reuse the shared
components and AgentPanel. Keep agent moments explicit and attributed to the Drafting Agent.