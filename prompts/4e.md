Re-read CLAUDE.md. Fix the "Signatures & Approvals" section of the recommendation document. Use plan
mode: analyze how it's currently produced/populated, propose the plan, implement after I approve.

PROBLEM: today the Drafting Agent generates the document with the Signatures & Approvals block ALREADY
FULLY FILLED with names. That's unrealistic — in the real PPC recommendation these are co-signatures
(προσυπογραφές) collected progressively as the document moves through its lifecycle, organized in
tiers (Hierarchy · Parallel Bodies · Preventive Financial Control · General Directors · Legal Counsel
/ Group GD Legal Affairs & Corporate Governance).

INTENDED behavior — the signature block fills PROGRESSIVELY, mirroring the process:
- On Drafting Agent generation: render the Signatures & Approvals section as a STRUCTURED SKELETON
  with the correct PPC tiers and roles, but every slot EMPTY / "Pending" (no names yet). It's a
  placeholder structure, not signed.
- Each slot then gets populated by the EVENT that justifies it, with the signer's name + role +
  timestamp, and visibly transitions Pending → Signed:
  - Business Unit owner (Hierarchy tier) → filled when the owner accepts the final version
    (UJ1 step 7, "All Reviews Completed").
  - Legal / Finance / Compliance → each filled when that function APPROVES in UJ2 (an approval =
    that function's co-signature appears; a "Returned for update" does NOT sign).
  - General Directors / Legal Counsel tier → filled at the Corporate Secretariat stage (UJ3) when
    the reco is prepared/ready for the BoD.
- Show overall progress (e.g. "3 of 6 approvals signed") and keep unsigned slots clearly "Pending",
  so the signature block doubles as a live approval-status indicator.

Constraints:
- Scripted/deterministic/click-driven per CLAUDE.md — names are pre-written FICTIONAL signers with
  realistic PPC-style titles (do not reuse the real people from the reference PDF). No live typing.
- Tie population to the EXISTING state transitions / store actions already in the journeys
  (acceptFinal, approveReview, the UJ3 readiness/prepare step) — do not invent a parallel flow.
- The reco↔resolution and audit log should reflect each signature event.
- Keep it consistent with the PPC document format (step 4d). The "direct to Chairman" path should
  show the relevant function slots remaining Pending (it bypassed them) — reinforcing, en passant,
  that reviews were skipped.
- Surgical change: only the signature/approval block behavior and its population on existing events.
  Don't rebuild the journeys or other sections.

After I approve the plan, implement it and show me the signature block starting empty and filling as
the hero reco moves BU → review → secretariat.