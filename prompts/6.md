Re-read CLAUDE.md. Build the Corporate Secretariat journey (UJ3) — the control tower — against the
shared store.

0. Control-tower dashboard: all recommendations with approval status and readiness at a glance.
   Filterable; pre-filter on Procurement for the demo.

1. Pipeline monitor: group recommendations into in-progress / overdue / ready-for-BoD.

2–3. Receive the submitted hero reco and run a completeness check: approvals present, document
   consistency, draft resolution present, readiness.

4. Agentic support: Recopilot's Readiness Agent assigns an animated readiness score (use the
   ReadinessMeter) and flags any residual gaps, in an AgentPanel.

5. Generate BoD pack: the Readiness Agent assembles an agenda and a document bundle. Render it
   nicely on screen; optionally add a "Download pack" button that produces a simple PDF in-browser
   (e.g. jsPDF) as a flourish.

6. Chairman touchpoint: a "Share with Chairman" modal showing content + approval status. Note here,
   en passant, the Chairman's discretion (may submit despite reservations, or defer). The Chairman
   is a touchpoint only — NOT a separate persona.

7. Final submission: "Submit to BoD" sets status "Submitted to BoD" and updates the pipeline.

Keep the Readiness Agent's reasoning explicit. Ensure the hero reco now reads end-to-end across all
three personas.