Re-read CLAUDE.md. Polish the agentic layer so the "under the hood = Recopilot orchestrating agents"
message is unmistakable, consistent, and delightful — without ever calling a live API.

- Standardize the AgentPanel across all three journeys: thinking shimmer, reasoning-step chips that
  appear in sequence, streamed result text, and a clear named-agent header with the agentic accent.
  Always use the descriptive agent names (Drafting Agent, Legal Review Agent, Finance Review Agent,
  Compliance Review Agent, Readiness Agent) — no code names anywhere in the UI.
- Make the "Under the Hood" toggle reveal a clean orchestration view (which Recopilot agent ran, its
  inputs, the ordered steps, and outputs) — this is the moment for the IT audience.
- Consistent agentic iconography and accent color, distinct from the brand accent.
- Add smooth framer-motion transitions for status changes, panel reveals, and persona switches.
- Polished empty / loading / success states everywhere.
- Verify all scripted agent timing feels lifelike but is instant-on-demand and deterministic — no
  real latency, nothing that can stall or fail on stage.