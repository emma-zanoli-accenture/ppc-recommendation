Re-read CLAUDE.md. Build the shared app shell (no journey logic yet, just the chrome and shared
components). Use the frontend-design skill.

1. Top bar: brand lockup "Recopilot · for PPC Group" (client name from the configurable constant),
   and a PROMINENT persona selector with three options: "Business Unit (Procurement)",
   "Legal / Finance / Compliance", "Corporate Secretariat". Selecting one switches the main view.
   This is how the presenter navigates the demo, so make it obvious and pleasant.

2. View switching for the 3 personas (placeholder content for now), all reading the same store.

3. Shared components (src/components), styled to the design system:
   - StatusBadge (status color system), AgentBadge (agent identity + agentic accent),
   - RecoCard (title, BU, status, deadline, reviewer chips),
   - ReadinessMeter (animated 0–100 score),
   - Timeline / AuditLog,
   - AgentPanel: the centerpiece of the agentic story — shows the named Recopilot agent (e.g.
     "Drafting Agent", "Legal Review Agent"), a thinking animation, reasoning-step chips appearing
     in sequence, and the streamed result. Powered by the useAgent hook from step 2. Include an
     "Under the Hood" toggle that reveals the orchestration detail (which agent Recopilot ran,
     inputs, steps, output) for the technical audience.

4. A toggleable "Demo Guide" panel (collapsible side panel): shows the current narrative step and a
   short "what to click next" hint, following the FILO LOGICO in CLAUDE.md. The presenter can hide
   it for the real run.

5. A "Reset Demo" button in the top bar wired to resetDemo().

Remove the temporary dev page from step 2. Make transitions smooth with framer-motion.